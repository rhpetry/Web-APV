[project]
name = "web-apv"
version = "0.1.0"
description = "FastAPI starter for ontology upload or triplestore connection input"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116.0",
    "language-tags>=1.2.0",
    "pydantic-settings>=2.10.1",
    "python-multipart>=0.0.20",
    "rdflib>=7.1.4",
    "uvicorn>=0.35.0",
]
