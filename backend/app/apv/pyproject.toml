[project]
name = "apv"
version = "0.1.0"
description = "CLI tool for verifying OWL ontologies (RDF) via local graphs or remote SPARQL endpoints."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "language-tags>=1.2.0",
    "rdflib>=7.6.0",
]
